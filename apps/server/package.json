{
  "name": "@agent-review/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/bin/mcp-http.js",
  "types": "dist/bin/mcp-http.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit",
    "mcp:http": "tsx src/bin/mcp-http.ts",
    "mcp:stdio": "tsx src/bin/mcp-stdio.ts"
  },
  "dependencies": {
    "@agent-review/shared": "0.1.0",
    "@fastify/static": "^8.2.0",
    "@modelcontextprotocol/sdk": "^1.21.0",
    "dotenv": "^17.3.1",
    "fastify": "^5.6.1",
    "zod": "^4.1.8"
  }
}

